# <Initiative Name>

**Slug:** `<initiative-slug>` (matches the app route `/<initiative-slug>`)
**Location:** <place · region · landscape>
**Status:** <planned / active / flagship>

Place-specific material for this initiative. Copy this folder to
`initiatives/<slug>/` and fill in. Keep location-agnostic material in `protocol/` instead.

Suggested sub-folders (add per-language `EN/ DE/ PL/ UK/` as needed):
- `whitepaper/` — the initiative's own vision/case document
- `funding/` — local grants, crowdfunding, partnerships
- `needs/` — particular requirements
- `media/` — photos, maps, local assets
