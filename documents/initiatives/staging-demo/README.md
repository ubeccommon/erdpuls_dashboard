# Erdpuls — Staging Demo

**Slug:** `staging-demo` (directory-card-only — no app route)
**Location:** — (not a real place)
**Status:** coming_soon (staging / demo)

> ⚠️ **This is a clearly-labelled staging/demo entry**, not a real initiative.
> It exists to validate the **data-driven initiatives directory** (open thread #3)
> end to end: `/` should list this card from `app/initiatives.py`, not from
> hardcoded HTML. The UK blurb is intentionally omitted so the card exercises the
> EN-fallback path. **Replace this with the real initiative #2** (fill in the real
> `Initiative(...)` in `app/initiatives.py` and rename this folder to the real slug).

Place-specific material would go here once this becomes a real initiative
(copy `_TEMPLATE/` conventions). Keep location-agnostic material in `protocol/`.
